barracuda.describe("Note Controller Test", function() {
  barracuda.it("exists and can return a list", function() {
    // let dummyNoteList = class {};
    // dummyNoteList.prototype = {
    //   getList: function() {
    //     return [];
    //   }
    // };
    // let noteController = new NoteController(dummyNoteList);
    // barracuda.expect(noteController.list.getList()).isArray();
  });
});
